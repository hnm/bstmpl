* + .ci-anchors,
* + .ci-attachment,
* + .ci-article,
* + .ci-image,
* + .ci-image-centered,
* + .ci-video,
* + .ci-accordion {
	margin-top: $common-padding;
}

/*
*	CI-Attachment
*/

// -- nothing --

/*
*	CI-Anchor
*/
.ci-anchors {
	display: flex;
	justify-content: center;
	width: 100%;
	margin-bottom: 0;
	.list-inline-item {
		&:not(:last-child) {
			margin-right: 10px;
		}
		a {
			display: block;
		}
	}
}

/*
*	CI-Article
*/
.ci-article-title a {
	text-decoration: none;
}
.ci-article-link {
	margin-bottom: 1rem;
	display: inline-block;
}

@include media-breakpoint-up(sm) {
	.ci-article .ci-article-image {
		margin-top: 0;
	}
}

/*
*	CI-Image
*/
.ci-image {
	figure,
	figure > :last-child {
		margin-bottom: 0;
	}
}

.ci-image + * {
	margin-top: $common-padding;
}

.ci-image figcaption {
	margin-top: 0.25rem;
	font-size: $font-size-base;
	color: $gray-light;
}

.ci-image > figure {
	display: inline-block;
}

.ci-image-portrait {
	text-align: center;
}

.ci-image-l {
	text-align: left;
}

.ci-image-c {
	text-align: center;
}

@include media-breakpoint-up(md) {
	.ci-image-r > figure {
		float: right;
	}

	.ci-image-portrait {
		> figure {
			width: 100% * (1 / $ci-image-aspect-ratio);
		}
	}

	.ci-image-l > figure,
	.ci-image-r > figure {
		> figcaption {
			text-align: left;
		}
	}

	.ci-image-l > figure,
	.ci-image-c > figure,
	.ci-image-r > figure {
		width: $ci-image-alignment-applied-width;
	}
}

.ci-accordion + * {
	margin-top: $common-padding;
}

.ci-accordion-title {
	background: gray;
	color: white;
	padding: 8px 10px;
	display: flex;
}

.ci-accordion-title:before {
	content: "+";
	width: 20px;
}

.ci-accordion-open .ci-accordion-title:before {
	content: "-";
}

.js .ci-accordion-content {
	display: none;
}

@if ($ci-accordion-enabled) {
	.ci-accordion-content {
		background: $gray-lightest;
		padding: 10px 10px 12px 30px;
	}
}

.ci-accordion-content > :last-child,
.ci-accordion-content > .ci-cke > :last-child {
	margin-bottom: 0;
}

// Video

// --- nothing ---

/*
*
* M U L T I  C O L U M N  L A Y O U T
*
*/
* + .ci-2-col,
* + .ci-3-col {
	margin-top: $common-padding;
}

@include media-breakpoint-down(sm) {
	// .ci-2-col,
	// .ci-3-col {
	// 	& > * + * {
	// 		margin-top: $common-padding;
	// 	}
	// }

	.ci-2-col > * + * {
		margin-top: $common-padding;
	}
	.ci-3-col > * + * {
		margin-top: $common-padding;
	}
}